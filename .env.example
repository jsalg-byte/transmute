# Public only: never put DATABASE_URL or any secret in this file.
# This will be the public Coolify URL for the standalone api/ service.
EXPO_PUBLIC_API_BASE_URL=https://api.your-transmute-domain.example
