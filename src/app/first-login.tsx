import { FirstLoginFlow } from '@/components/first-login-flow';

/** Post-auth orientation. This route will be entered after the approved mobile auth flow is added. */
export default function FirstLoginScreen() {
  return <FirstLoginFlow />;
}
