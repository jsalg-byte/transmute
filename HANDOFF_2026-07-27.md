# Transmute handoff — July 27, 2026

## Source of truth

**Work only in this repository:** `/Users/mzootfb/Sites/transmute-mobile`.

This is the active Transmute app and is an Expo Router project shared across web, iOS, and Android. The browser development server is Expo on **http://localhost:8081**. Do not use `/Users/mzootfb/Sites/gym-micro` for new Transmute product work; it is the legacy Next.js project and must not receive duplicated redesign work.

The Expo project is intentionally independent of the Next app. Its standalone Fastify API in `api/` is the server-side boundary to the deployed Coolify Postgres database. The client must never connect to Postgres, R2, or legacy NextAuth directly.

## Current architecture

- Client routes live in `src/app/`.
- Shared authenticated UI/data work is concentrated in `src/components/record-screen.tsx`.
- The API client is `src/lib/api.ts`.
- API implementation is `api/src/index.ts`.
- Public browser/native API base URL is `EXPO_PUBLIC_API_BASE_URL` in `.env.local`; it must point to the deployed Transmute API, not a local API.
- API auth uses access/refresh tokens and Expo SecureStore. Do not use browser-cookie or NextAuth assumptions.

## Route inventory

Implemented Expo routes include:

```text
/                         Guest three-step onboarding
/sign-in                  Username/password login
/sign-up                  Registration
/first-login              Four-step first-login orientation
/dashboard                Signed-in home
/workout-plans            Plans and days
/exercises                Exercise library
/sessions                 Session history/start flow
/sessions/[id]            Active/completed session
/sessions/[id]/share      Screenshot-friendly workout record
/nutrition                Nutrition
/fasting                  Fasting
/progress                 Progress
/friends                  Friend management
/settings                 Settings
/admin                    Admin
```

## Recent confirmed Expo work

The active Expo server was restarted successfully on July 27 and is listening on `http://localhost:8081`.

The guest onboarding component is `src/components/onboarding-flow.tsx`.

- It is the component used for `/`.
- The three slides are Nigredo, Albedo, and Rubedo.
- The first two `Continue` screens now include an explicit spacer matching the final screen’s sign-in prompt (`marginTop: 16`, `height: 17`). This is meant to align the `Continue` and `Create account` primary buttons on the same baseline.
- This latest change was made in **Transmute**, not Gym Micro.

Checks run after that change:

```sh
npm run lint
npx tsc --noEmit
git diff --check
```

All completed without errors.

## Critical correction: nutrition

The requested nutrition information architecture redesign was mistakenly implemented in `/Users/mzootfb/Sites/gym-micro`, not in this Expo repository. **It has not been implemented in Transmute.**

The next chat should treat the following as a fresh, Expo-only implementation task:

1. Inspect the real Expo nutrition route, `record-screen.tsx`, `src/lib/api.ts`, and the actual API endpoints before changing UI/data mapping.
2. Implement the requested nutrition structure in Transmute only: Today, Foods, and History as separate focused surfaces; log meal and food creation as focused sheets/flows.
3. Preserve existing real API capabilities: barcode lookup (`GET /v1/barcodes/:code`), nutrition-label parser (`POST /v1/nutrition-label/parse`), food creation (`POST /v1/foods`), meal logging (`POST /v1/meals`), and meal-photo presign/finalization routes.
4. Do not copy Next components or guess API field names. Read `api/src/index.ts` and the existing client contracts first.

## Existing API surface (verified from `api/src/index.ts`)

Relevant nutrition endpoints:

```text
GET  /v1/barcodes/:code
POST /v1/nutrition-label/parse
POST /v1/foods
POST /v1/meals
POST /v1/meals/:id/photo/presign
POST /v1/meals/:id/photo
```

Other major endpoints exist for auth, plans/days, exercises, sessions/sets, fasting, friends, preferences, progress, admin, and the combined record response (`GET /v1/record`). Treat the API code as the contract source of truth.

## Current working-tree state

The Transmute worktree is **dirty**. Preserve all existing edits and do not reset, clean, or overwrite unrelated work. At handoff time, `git status --short` included:

```text
M  api/src/index.ts
M  app.json
M  package-lock.json
M  package.json
M  src/app/_layout.tsx
M  src/app/sessions/[id].tsx
M  src/app/shared-sessions/[id].tsx
M  src/components/onboarding-flow.tsx
M  src/components/record-screen.tsx
M  src/components/ui/collapsible.tsx
M  src/lib/api.ts
?? achieve.md
?? api/src/calistree.ts
?? arcana.md
?? ranks.md
?? src/app/sessions/[id]/
?? src/components/muscle-heat-map.tsx
?? src/lib/calistree-guides.ts
?? src/types/react-muscle-highlighter-assets.d.ts
```

Do not commit, push, or deploy unless explicitly requested.

## Operating rules for the next chat

- Use `/Users/mzootfb/Sites/transmute-mobile`, never Gym Micro, for Transmute changes.
- Keep Expo web testing on port 8081. Start/restart with `npm run web` from this repository.
- The user performs browser verification; run lint/type/diff/build checks but do not open a browser for verification.
- Keep the Transmute visual direction: warm `#F4EFE7` ground, `#101015` ink, square buttons/cards, Lucide icons, no emojis, and original alchemical influence without copying Fullmetal IP.
- Use the deployed Transmute API set by `EXPO_PUBLIC_API_BASE_URL`; do not start or substitute a local API when the user asks to use deployed data.
- Before any data-display work, inspect the verified API contract and current response shape. Do not add broad field-name fallback chains.

## Useful commands

```sh
# Expo web development server
npm run web

# Client quality checks
npm run lint
npx tsc --noEmit
git diff --check

# API quality checks
npm run api:typecheck
npm run api:build

# Static web production check
npx expo export --platform web
```
